node{
     stage('build'){
                     git 'https://github.com/sasidevops/myweb'
                    }
     stage('Maven Package'){
                            def mavenHome = tool name: 'maven-1', type: 'maven'
                            sh "${mavenHome}/bin/mvn package"
                   
                         }
      stage('Delpoy to tomcat'){
      
                              sshagent(['tomcat-dev']) {
                              sh '-o StrictHostKeyChecking=no target/*.war ec2-user@172.31.30.136:/opt/tomcat8/weapps'
}
}
      
      }                
                      
     
          
                    
